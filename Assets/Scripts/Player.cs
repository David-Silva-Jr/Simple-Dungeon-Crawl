using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : BaseActor
{
    public Player(float moveSpeed, float turnSpeed, float maxHp, float hp, string name) : base(moveSpeed, turnSpeed, maxHp, hp, name) {
        
    }
}
